export default {
    SET_RESPONSE (state, response) {
        state.response = response
    },
    SET_USER (state, user) {
        state.user = user
    },
    SET_TOKEN (state, token) {
        state.token = token
    }

}
